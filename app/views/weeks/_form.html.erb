<%= form_with(model: week, local: true) do |form| %>
  <% if week.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(week.errors.count, "error") %> prohibited this week from being saved:</h2>

      <ul>
      <% week.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :excersize %>
    <%= form.text_area :excersize, id: :week_excersize %>
  </div>

  <div class="field">
    <%= form.label :workout_id %>
    <%= form.number_field :workout_id, id: :week_workout_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
